# ── Core web framework ──────────────────────────────────────────────────────
fastapi>=0.111.0        # Web framework — automatic validation, async, OpenAPI docs
uvicorn[standard]>=0.30.0  # ASGI server (replaces Flask dev server)
gunicorn>=22.0.0        # Process manager — wraps uvicorn workers in production
python-multipart>=0.0.9 # Required by FastAPI for form parsing
jinja2>=3.1.4           # HTML template rendering (FastAPI uses this for TemplateResponse)
itsdangerous>=2.2.0     # Signs session cookies (used by SessionMiddleware)

# ── Data & ML ────────────────────────────────────────────────────────────────
pandas>=2.2.3
numpy>=2.1.0
scikit-learn>=1.5.2
joblib>=1.4.2
openpyxl>=3.1.5

# ── Explainability ───────────────────────────────────────────────────────────
shap>=0.46.0

# ── Agentic AI / LangGraph ───────────────────────────────────────────────────
langgraph>=0.1.19
langchain>=0.2.6
langchain-community>=0.2.6
langsmith>=0.1.77

# ── Database ─────────────────────────────────────────────────────────────────
psycopg2-binary>=2.9.9

# ── External API calls ───────────────────────────────────────────────────────
requests>=2.32.3