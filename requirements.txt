# ── Core web framework ──────────────────────────────────────────────────────
flask==3.0.3          # Web framework — serves the UI and REST API
gunicorn==22.0.0      # Production WSGI server (replaces Flask dev server on Render)

# ── Data & ML ───────────────────────────────────────────────────────────────
pandas>=2.2.3         # Data loading and manipulation (Excel files, DataFrames)
numpy>=2.1.0          # Numerical operations (array math, SHAP output processing)
scikit-learn>=1.5.2   # RandomForest model, StandardScaler, NearestNeighbors
joblib>=1.4.2         # Serialize/deserialize model and SHAP explainer to disk
openpyxl>=3.1.5       # Read .xlsx files (required by pandas for Excel support)

# ── Explainability ──────────────────────────────────────────────────────────
shap>=0.46.0          # SHAP TreeExplainer — per-prediction feature attributions

# ── Agentic AI / LangGraph ──────────────────────────────────────────────────
langgraph>=0.1.19          # Orchestrates the multi-agent graph (Investigator + Communicator)
langchain>=0.2.6           # Core LangChain primitives used by LangGraph
langchain-community>=0.2.6 # Community integrations (used by langchain internally)
langsmith>=0.1.77          # Observability — traces every agent run end-to-end

# ── External API calls ──────────────────────────────────────────────────────
requests>=2.32.3      # HTTP client — calls Hugging Face Inference API + Nominatim geocoding
psycopg2-binary>=2.9.9 